# Core detection and CV
ultralytics>=8.3.0
opencv-python-headless>=4.10.0.84

# API backend
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Dashboard
streamlit>=1.40.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# HTTP client (dashboard → API)
requests>=2.32.0

# TODO: Pin exact versions after running:
#   pip install -r requirements.txt
#   pip freeze > requirements_pinned.txt
# Replace this file with the pinned output before Phase 3 begins.
